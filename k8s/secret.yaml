apiVersion: v1
kind: Secret
metadata:
  name: player-secret
  labels:
    app: player
type: Opaque
stringData:
  # Set this to the desired admin password before applying.
  ADMIN_PASSWORD: "changeme"
