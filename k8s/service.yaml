apiVersion: v1
kind: Service
metadata:
  name: kiss-media-player
  labels:
    app: kiss-media-player
spec:
  type: ClusterIP
  selector:
    app: kiss-media-player
  ports:
    - port: 8080
      targetPort: http
      name: http
